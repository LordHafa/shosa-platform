<template>
  <header class="bg-navy text-white shadow-lg">
    <nav class="mx-auto flex max-w-7xl flex-col gap-4 px-4 py-4 md:flex-row md:items-center md:justify-between">
      <router-link to="/" class="flex items-center gap-3">
        <img src="/assets/reference/seeta-reference-logo.jpeg" alt="Seeta Alumni logo" class="h-12 w-12 rounded-full border-2 border-gold bg-white object-cover" />
        <span>
          <span class="block text-xl font-bold leading-tight">Seeta Alumni</span>
          <span class="block text-xs font-semibold text-gold">Website + SACCO</span>
        </span>
      </router-link>

      <div class="flex flex-wrap items-center gap-3 text-sm font-semibold">
        <router-link class="hover:text-gold" to="/">Home</router-link>
        <router-link class="hover:text-gold" to="/about">About</router-link>
        <router-link class="hover:text-gold" to="/events">Events</router-link>
        <router-link class="hover:text-gold" to="/gallery">Gallery</router-link>
        <router-link class="hover:text-gold" to="/store">Store</router-link>
        <router-link class="hover:text-gold" to="/contact">Contact</router-link>
        <router-link v-if="auth.isAdmin" class="rounded-lg bg-white/10 px-3 py-1 hover:text-gold" to="/admin">Admin</router-link>
        <router-link v-if="auth.isAlumni" class="rounded-lg bg-white/10 px-3 py-1 hover:text-gold" to="/alumni/dashboard">Dashboard</router-link>
        <router-link v-if="!auth.isLoggedIn" class="rounded-lg bg-gold px-4 py-2 text-navy" to="/login">Login</router-link>
        <button v-else class="rounded-lg bg-red-600 px-4 py-2" @click="logout">Logout</button>
        <button class="rounded-lg border border-white/30 px-3 py-2" @click="theme.toggleTheme()">{{ theme.theme === 'light' ? 'Dark' : 'Light' }}</button>
      </div>
    </nav>
  </header>
</template>

<script setup>
import { useRouter } from 'vue-router'
import { useAuthStore } from '../stores/auth'
import { useThemeStore } from '../stores/theme'

const router = useRouter()
const auth = useAuthStore()
const theme = useThemeStore()
function logout() { auth.logout(); router.push('/') }
</script>




